# LangGraph ? Agent ???????

???????????? LangGraph ? Agent ????????

??????????????? FastAPI ?? API?? PostgreSQL ????????? LangGraph ??????????????????????????????????? Agent?Workflow ????????

## ????

- FastAPI ????
- Crew / Agent / Conversation / Message ??
- ?????? `POST /api/chat`
- `chat` ???? `supervisor_simple` ???
- ??????????
- PostgreSQL ????
- SQLAlchemy ??? service ?
- ???????? crew/agent -> chat -> message ??

## ????

```text
HTTP API
  -> Conversation / Crew / Agent services
  -> WorkflowService
  -> supervisor_simple workflow
  -> supervisor agent graph
  -> messages / activity logs
```

`supervisor_simple` ???????????????? supervisor agent graph?

```text
analyze_input
  -> answer_directly
  -> create_plan
  -> assign_tasks
  -> check_status
  -> combine_results
```

?????? Supervisor ????????????????????????????????

## API ??

?????????

```text
GET    /api/health

GET    /api/crews/
POST   /api/crews/
GET    /api/crews/{crew_id}
PUT    /api/crews/{crew_id}
DELETE /api/crews/{crew_id}

GET    /api/agents/
POST   /api/agents/
GET    /api/agents/{agent_id}
PUT    /api/agents/{agent_id}
DELETE /api/agents/{agent_id}

GET    /api/conversations/
POST   /api/conversations/
GET    /api/conversations/{conversation_id}
PUT    /api/conversations/{conversation_id}
DELETE /api/conversations/{conversation_id}
GET    /api/conversations/{conversation_id}/messages
POST   /api/conversations/{conversation_id}/messages
POST   /api/conversations/{conversation_id}/chat
POST   /api/conversations/{conversation_id}/chat/stream

POST   /api/chat
```

`POST /api/chat` ??????

- ? `conversation_id`????????
- ?? `conversation_id`???? `user_id` ? `crew_id`???????????????
- ????? `conversation_id`??????????

## ????

`POST /api/conversations/{conversation_id}/chat/stream` ?? SSE?

???????????????????

```text
workflow.started
workflow.node.started
workflow.node.completed
workflow.task.assigned
workflow.agent.error
workflow.completed
chat.completion.chunk
[DONE]
```

????????????????????? LangGraph state?

```text
node -> emit_event(...) -> WorkflowEventSink -> /chat/stream -> SSE
```

## ????

```text
app/
  agents/
    supervisor/              # Supervisor agent graph?state?nodes?prompts
  api/
    routes/
      conversation.py        # Conversation ? chat API
      crew.py                # Crew ? Agent API
      storage.py             # Storage API
  core/
    langgraph/
      events.py              # ??? workflow event sink
      workflows/
        registry.py          # Workflow registry
        supervisor_simple/   # ???????
        adapters/            # Agent graph / workflow adapter
  db/                        # SQLAlchemy base ? session
  models/                    # Crew?Agent?Conversation?ActivityLog
  schemas/                   # Pydantic schemas
  services/                  # Crew?Conversation?Workflow?AI Provider

database/
  schema.sql                 # PostgreSQL schema ??

tests/
  api/
    test_chat_e2e.py         # ??? API ?????
  services/
```

## ????

### 1. ????

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

### 2. ??????

```bash
copy .env.example .env
```

???????

```env
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/multiagent_db"
DATABASE_SCHEMA="public"
OPENROUTER_API_KEY="..."
JWT_SECRET_KEY="change-me"
```

### 3. ??????

???? `database/schema.sql` ??? PostgreSQL schema??????????? SQLAlchemy metadata ????

### 4. ????

```bash
uvicorn app.main:app --reload
```

?????

```text
http://localhost:8000/api/docs
http://localhost:8000/api/redoc
```

## ??????

1. ???? crew?
2. ? crew ???? supervisor agent?
3. ? crew ??????? worker agent?
4. ?? `POST /api/chat`?
5. ? `GET /api/conversations/{conversation_id}/messages` ???????

????????????????????????

## ??

```bash
pytest
```

???????

- crew service
- agent service
- conversation/chat API
- ?? workflow ? chat ??
- SSE ???????
- ??? crew -> agents -> chat -> messages ??

## ????

????????????? LangGraph ? Agent ????????

?????? Dify?Flowise?Langflow ???????????????????

```text
???????
?? workflow registry
??? agent/workflow ????
????????
?????????
?? fork ?????
```

## ???

MIT
